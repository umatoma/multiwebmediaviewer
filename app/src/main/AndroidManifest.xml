<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="io.github.umatoma.multiwebmediaviewer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="io.github.umatoma.multiwebmediaviewer.view.hatenaEntry.HatenaEntryActivity" />
        <activity
            android:name="io.github.umatoma.multiwebmediaviewer.view.home.HomeActivity"
            android:launchMode="singleTask" />
        <activity android:name="io.github.umatoma.multiwebmediaviewer.view.hatenaAuth.HatenaAuthActivity" />
        <activity android:name="io.github.umatoma.multiwebmediaviewer.view.start.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>